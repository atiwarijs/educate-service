server:
  port: 8086
  servlet:
    context-path: /api/v1/school
spring:
  application:
    name: classes-service
  profiles:
    active: pgsql

# Eureka server config
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
